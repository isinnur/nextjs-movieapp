"use client";
import React from "react";
import { Spinner } from "@nextui-org/react";
const Loading = () => {
  return (
    <div>
      <Spinner />
      Loading..
    </div>
  );
};

export default Loading;
